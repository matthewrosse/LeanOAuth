# LeanOAuth.Benchmarks

BenchmarkDotNet project. Not packaged, not built or run by CI. Run manually:

```
dotnet run -c Release --filter '*'
```

See [RESULTS.md](RESULTS.md) for recorded results and the RSA lock conclusion.
